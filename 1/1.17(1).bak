(define (* a b)
  (if (= b 0)
      0
      (+ (double a)(* a (halve b)))))

(define (double a)
  (+ a a))

(define (halve a)
  (cond (even? a) (/ a 2)))

(define (even? n)
  (= (remainder n 2) 0))


(* 2 2)


